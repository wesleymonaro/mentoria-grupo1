import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#FFF',
        flex: 1,
        padding: 12,
        
    },

    title: {
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 24
    }
});

export default styles;